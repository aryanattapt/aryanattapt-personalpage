const { Fragment } = require("react")
import SimpleSidebar from "@/components/templates/sidebar";

const HomePage = () => {
    return <Fragment>
        <SimpleSidebar/>
    </Fragment>
}

export default HomePage;